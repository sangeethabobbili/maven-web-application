version: '3.1'
services:
  springboot:
    image: dockergeethaacc/maven-web-application:VERSION
    restart: always
    ports:
    - 8080:8080
    networks:
    - mavenappbridge
networks:
  mavenappbridge:
